# calc-serv

Serv-версия калькулятора (параллельная ветка). Основная — https://github.com/BelMihMed/calc

## Проверка формул

Тесты читают реальный `index.html`, извлекают из него сценарии и проверяют дефолтные суммы, а также дефолтные стоимости настройки от интегратора:

```bash
npm test
```
